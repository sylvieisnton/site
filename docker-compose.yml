services:
    site:
        image: personal-site
        build: .
        restart: unless-stopped
        ports:
            - "3000:3000"
        volumes:
            - ./config/profile.json:/app/src/data/profile.json:ro
            - ./config/bot.json:/app/src/data/bot.json:ro
            - ./config/content.json:/app/src/data/content.json:ro
            - ./config/pfp.png:/app/src/public/pfp.png:ro
        env_file: .env
        cap_drop: [ALL]
        read_only: true
        security_opt:
            - no-new-privileges:true
        tmpfs:
            - /tmp
